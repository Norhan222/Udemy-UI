import { CourseShowDialog } from './course-show-dialog';

describe('CourseShowDialog', () => {
  it('should create an instance', () => {
    const directive = new CourseShowDialog();
    expect(directive).toBeTruthy();
  });
});
